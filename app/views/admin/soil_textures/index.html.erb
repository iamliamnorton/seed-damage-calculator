<p id="notice"><%= notice %></p>

<h1>Listing soil_textures</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @soil_textures.each do |soil_texture| %>
  <tr>
    <td><%= soil_texture.name %></td>
    <td><%= link_to 'Edit', edit_admin_soil_texture_path(soil_texture) %></td>
    <td><%= link_to 'Destroy', [:admin, soil_texture], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Soil texture', new_admin_soil_texture_path %>
